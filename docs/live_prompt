# Prompt for the on-rig HIL agent — CURRENT (supersedes all earlier versions)

This file is rewritten in place after every session; it is always the single
current prompt. Copy everything below this line into the agent session
driving the rig. Do not look for or act on any earlier version.

---

You are continuing the debugger-forced reset-type test campaign. State of
knowledge as of 2026-08-07:

## Established (do not re-derive)

- The failed `--type system` run is ROOT-CAUSED, no reset ever occurred:
  the TRACE32 `Data.Set` to SCU_SWRSTCON is a Cerberus/DAP (bus-master
  TAG 28) write into ACCEN-gated space that authorizes only CPU masters, so
  it was refused with a bus error. The refusal raised ALM7[20] (SPB Bus
  Error Event, FSP directly enabled on it), FSP assertion raised ALM10[18]
  (RT1-mapped), the CPU was left halted (Break with no Go on the success
  path - a confirmed script bug), so RT1 was never serviced and expired
  into ALM10[17] (~20 ms at default prescaler). Captured live:
  SMU_AG7=0x00100000, SMU_AG10=0x00060000, contactors/CAN knocked out by
  the safety reaction, stored reset-cause unchanged because nothing
  rebooted. The RSTCON preselect write is NOT affected (ENDINIT-only
  protection, TRACE32 auto-override handles it; readback-confirmed live).
- PROHIBITED from now on: any debug-port write to SCU_SWRSTCON, and any
  debug-port write into ACCEN-protected space generally. Every attempt
  fires ALM7[20]+FSP and drops the DUT. This includes "retry once to
  check".
- The warm variant remains untested and ON HOLD - not this slot.

## Open verification items (close these first, they are cheap)

1. One non-intrusive attach (NoDebug then Attach). Live-read and record:
   - SCU_ACCEN0 (0xF00363FC) - expected 0x66 (TAG 1,2,5,6 = CPU masters
     only; TAG 28 absent). This closes the last inferred link.
   - SMU_ACCEN0 - same question for the earlier SMU_AG8 bus error.
   - SMU_AG0..AG11 (confirm the rig is alarm-clean before any new work;
     if not clean, record and STOP - do not clear before recording).
   - SMU_RTC, SMU_RTAC00/01/10/11, SMU_STS (RTS/RTME bits).
   - TRACE32 settings: SYStem.Option.DOWNMODE,
     SYStem.Option.EndInitProtectionOverride (record, do not change).
   Then NoDebug. If the attach fails: probe-health check, STOP.

## Redesigned injection for system/application (validate this slot if the
## DUT is healthy and step 1 is clean)

The trigger must carry a CPU bus-master TAG. Debugger keeps only the roles
proven to work: RSTCON.SW preselect + readback, and post-reset evidence
reads. Two candidate paths, in preference order:

A. Firmware-executed trigger via Var.Call: with symbols loaded, call the
   firmware's own reset-request function (iLLD-style performReset or the
   project equivalent) so the SWRSTCON write executes on a CPU (passes
   ACCEN, firmware handles CPU-ENDINIT) and the CPU is RUNNING when the
   reset fires (avoids errata OCDS_TC.H015/H018). Sequence: preselect
   RSTCON.SW via debugger, readback, Go (CPU must be running), then
   Var.Call the trigger, immediately NoDebug, wait wake-up time, attach
   fresh, read stored cause + register set.
B. Bench-triggered via UDS ECUReset: preselect RSTCON.SW via debugger,
   drop to NoDebug BEFORE the trigger, command the reset over UDS, wait,
   attach fresh, read. Weakest debugger involvement, production trigger
   path - use this if Var.Call setup is not available this slot.

Acceptance for either path: stored reset-cause changes from baseline and
decodes to the injected type; live RSTSTAT and all AG groups clear after
boot; DUT answers UDS; evidence CSV written. Also explicitly record which
path (A or B) was used.

## Safety rails

- One non-resetting attach per step; on failure run the probe-health check
  and STOP. No SYStem.Up, no SYStem.Mode Down, no retry storms.
- Never clear an alarm or reset-status register before it is recorded.
- CPU must be RUNNING whenever a reset is expected to fire (H015/H018).
- Leave the DUT in Standby and the probe detached (NoDebug) at the end.
- Anything unexpected: stop, record, report - do not improvise.

## Deliverable

Short write-up: the ACCEN0 values (closes or reopens the root cause), the
alarm-clean check result, which redesigned path was validated and its full
register evidence, and any deviation. If ACCEN0 does NOT read 0x66, say so
loudly - the root-cause chain has one inferred link and that reading is
its test.
