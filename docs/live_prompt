# Prompt for the on-rig HIL agent — CURRENT (supersedes all earlier versions)

This file is rewritten in place after every session; it is always the single
current prompt. Copy everything below this line into the agent session
driving the rig. Do not look for or act on any earlier version.

---

You are continuing the debugger-forced reset-type test campaign. State of
knowledge as of 2026-08-07 (late update, after the live ACCEN reads and the
UDS scoping check):

## Established — verified live or against the UM (do not re-derive)

- Root cause of the failed `--type system` run is CLOSED: SCU_ACCEN00
  live-reads 0x00000066 (bus-master TAGs 1,2,5,6 = CPU0/CPU1 only; TAG 28
  Cerberus/DAP excluded). The debug-port SWRSTCON write was refused →
  ALM7[20] "SBCU SPB Bus Error Event" (FSP enabled on it) → FSP assertion →
  ALM10[18] (RT1-mapped) → CPU halted (script bug) → RT1 expired →
  ALM10[17] → safety cascade dropped contactors/CAN. No reset ever
  occurred. SMU_ACCEN0 live-reads 0x00000006 (CPU0-only) — this also
  closes the earlier SMU_AG8 bus-error question: same exclusion.
- UM register facts: SCU_SWRSTCON write = "SV,E,P0"; SCU_RSTCON write =
  "SV,SE,P0". BOTH ACCEN00-gated; ENDINIT override cannot spoof the master
  TAG. Refused writes are error-acknowledged; reads are always processed.
- PERMANENT RULE for this hardware config: no debugger writes into
  ACCEN-protected space, ever - including the RSTCON preselect. Reads only.
- UDS ECUReset is RULED OUT for class selection: the bench UDS service
  exposes only ISO hardReset=1 / softReset=3 with no parameter mapping to
  RSTCON.SW; the firmware Dcm mapping is unverified and out of scope. Do
  not use UDS to claim system-vs-application coverage. (Context: the
  AUTOSAR-standard Mcu_PerformReset API is also argument-less - reset
  class comes from static config - so runtime class selection at the MCAL
  layer is not a given either.)
- Still open, one cheap read: SCU_RSTCON baseline value in normal
  operation (settles whether the failed run's preselect readback was
  vacuous). Capture it first next slot, for the record.
- The warm variant remains untested and ON HOLD.

## Next slot — injection-path feasibility, reads and dry mechanics first

Precheck (all read-only, one non-intrusive attach): SMU_AG0..AG11
alarm-clean check (if not clean, record and STOP), SCU_RSTCON baseline,
then proceed.

1. SYMBOL SURVEY (read-only). With symbols loaded (/NoCode), search the
   symbol table for callable reset-request candidates:
   - iLLD-style: *ScuRcu*performReset*, *performReset*
   - MCAL/AUTOSAR: Mcu_PerformReset, Mcu_17_* reset variants
   - project wrappers: *SwReset*, *EcuReset*, *ResetRequest*, the Dcm
     reset handler
   Record exact symbol names, addresses, and (from types) prototypes.
   Also record whether any candidate takes a reset-class argument.
2. VAR.CALL MECHANICS PROOF (harmless). Before any one-shot reset call,
   prove the Var.Call toolchain on this build by calling a side-effect-free
   getter and checking the return value. The reset call is unobservable if
   the calling convention is wrong - validate mechanics on something
   recoverable first. CPU must be RUNNING for the call.
3. PATH SELECTION, in preference order:
   A. Var.Call a firmware reset-request function WITH a class argument
      (iLLD-style performReset(type)) - preselect and trigger in one
      CPU-context call. Sequence: CPU running, Var.Call, immediately
      NoDebug, wait wake-up time, attach fresh, read stored cause + full
      register set.
   B. CPU-EXECUTED RAM STUB (no firmware symbol needed): download a
      minimal stub that does Safety-ENDINIT unlock -> RSTCON.SW = class ->
      ENDINIT restore -> SWRSTCON.SWRSTREQ, and let the CPU execute it.
      The writes then carry a CPU master TAG (passes ACCEN00) and the CPU
      is running when the reset fires. No context restore needed - the
      chip reboots. Keep the stub minimal; do not park the CPU halted at
      any point where a reset can fire.
   C. If neither is viable this slot (no symbol, stub not qualified):
      STOP and escalate - system/application injection has no safe
      mechanism on this hardware without firmware-side collaboration
      (confirm the Dcm hard/soft-reset to RSTCON-class mapping in firmware
      source, or add a dedicated test hook). Record that conclusion as the
      slot outcome; do not improvise a fourth path.
   Acceptance for A or B: stored reset-cause changes from baseline and
   decodes to the injected type; live RSTSTAT and all AG groups clear
   after boot; DUT answers UDS; evidence CSV written; path used recorded.

## Safety rails

- One non-resetting attach per step; on failure run the probe-health check
  and STOP. No SYStem.Up, no SYStem.Mode Down, no retry storms.
- No debugger writes to ACCEN-protected space. Reads only, plus Var.Call /
  stub execution which run in CPU context by design.
- Never clear an alarm or reset-status register before it is recorded.
- CPU must be RUNNING whenever a reset is expected to fire (errata
  OCDS_TC.H015/H018).
- Leave the DUT in Standby and the probe detached (NoDebug) at the end.
- Anything unexpected: stop, record, report - do not improvise.

## Deliverable

Short write-up: SCU_RSTCON baseline value and the vacuous-readback
verdict; the symbol-survey table (names, addresses, prototypes, class
argument yes/no); Var.Call mechanics-proof result; which path (A/B/C) was
taken and its full register evidence or the escalation record. If any
established fact above is contradicted by a live reading, say so loudly
and stop.
