# Prompt for the on-rig HIL agent — CURRENT (supersedes all earlier versions)

This file is rewritten in place after every session; it is always the single
current prompt. Copy everything below this line into the agent session
driving the rig. Do not look for or act on any earlier version.

---

You are continuing the debugger-forced reset-type test campaign. State of
knowledge as of 2026-08-07 (post detailed-blocker report):

## Campaign status by requirement — do not re-derive

- <sysreq-warm> (warm PORST): PASSED on hardware 2026-07-03 (18/18
  registers). The debugger-forced warm variant is RETIRED unless
  re-evidence is explicitly requested.
- <sysreq-system> (system reset): NOT BLOCKED. The firmware writes RSTCON
  exactly once, in Mcu_Init() from static config, and it statically
  selects SYSTEM class (RSTCON=0x100, live-confirmed on two rigs). The
  implemented, proven UDS hardReset (subfunction 0x01) therefore produces
  a genuine system reset - zero protected writes, no CPU halt. This also
  closes the old open item: the failed run's RSTCON-readback guard passed
  vacuously (readback matched because the value was already there).
- <sysreq-application> (application reset): BLOCKED, Path C, with
  evidence: no production code path selects a reset class (symbol sweep:
  Mcu_PerformReset is void/void, only sets SWRSTREQ; UDS layer accepts
  only 0x01/0x81 and discards the subfunction after validation), the
  debugger cannot write RSTCON (ACCEN00=0x66, TAG 28 excluded, refusal
  itself raises a bus-error alarm), and the RAM-stub route is RETIRED -
  see below. Unblocking requires firmware-side collaboration: either a
  dedicated CPU-context test hook, or (cheap, recommended ask) a
  test-build config variant with McuResetCfg set to APPLICATION class so
  the existing proven UDS reset produces it. Note in evidence that such a
  build is a test-config variant; the decode logic under test is
  unchanged.

## Stub route retired — and a new hard rule

- Halting CPU0 while the firmware runs is DESTRUCTIVE on this build:
  proven live - the halt window latches AG7[17] (XBAR0 bus error) and
  AG10[17] (RT1 expiry; RT servicing lives on CPU0). Any reset fired from
  a staging halt carries contaminated cause evidence even if it works.
- A debugger-placed stub entered from a user-mode RTOS task context traps
  on the Safety-ENDINIT unlock (supervisor-only); hand-setting PSW is
  unqualified. Do not attempt stubs or Var.Call on this firmware.
- HARD RULE: do not halt CPU0 while the DUT is in normal operation. Reads
  via runtime access only.

## This slot, in order

1. PRE-RESET EVIDENCE CAPTURE (reads only - the coming reset erases the
   latched state): full SMU_AG0..AG11, SMU_STS (RTS/RTME), live
   SCU_RSTSTAT, stored reset-cause words, plus the session notes of the
   halt incident. Nothing is cleared by hand - recording first is the
   whole point.
2. FIRE UDS hardReset (0x01). Expected: genuine system reset; startup SW
   clears the latched alarms. This run doubles as (a) a clearing-clause
   bonus specimen - startup recovery from a heavily-latched alarm state -
   and (b) a PROVISIONAL <sysreq-system> decode specimen (document that
   the pre-reset baseline was dirty).
3. POST-RESET VERIFICATION (attach fresh, reads only): stored cause
   decodes SYSTEM_RESET; live RSTSTAT cleared; AG0..AG11 all clear; DUT
   answers; recover to normal operation.
4. FORMAL <sysreq-system> RUN: repeat the UDS hardReset once more from
   the now-clean baseline as the formal evidence specimen. Evidence CSV
   for both runs, before/after register sets, exact commands.
5. RECORD THE <sysreq-application> ESCALATION as the slot outcome: the
   firmware-collaboration ask (test hook, or the McuResetCfg=application
   config variant - the cheap option needing no test-hook code). Do not
   improvise further injection attempts.
6. RAISE SEPARATELY (fleet-wide, tangential): the ECUReset-behavior test
   that uses softReset=3 asserts a timer reset in its step 8, but the
   firmware rejects subfunction 3 as SUB_FUNCTION_NOT_SUPPORTED - so that
   step is silently vacuous or failing. Same vacuous-pass family as the
   RSTCON guard. File it; do not fix in-session.

## Safety rails

- Reads only over the debug port; no halting CPU0 in normal operation; no
  stubs, no Var.Call, no writes to ACCEN-protected space.
- Never clear an alarm or status register by hand; resets clear state via
  the startup path, which is itself evidence.
- One non-resetting attach per step; on failure run the probe-health
  check and STOP.
- Leave the DUT in Standby and the probe detached (NoDebug) at the end.
- Anything unexpected: stop, record, report - do not improvise.

## Deliverable

Short write-up: the pre-reset capture, both hardReset runs' evidence
(provisional + formal), the post-reset clearing verification, the recorded
application-class escalation text, and the separately-filed vacuous-test
finding. If any established fact above is contradicted live, say so loudly
and stop.
