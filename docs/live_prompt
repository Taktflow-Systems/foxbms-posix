# Prompt for the on-rig HIL agent — CURRENT (supersedes all earlier versions)

This file is rewritten in place after every session; it is always the single
current prompt. Copy everything below this line into the agent session
driving the rig. Do not look for or act on any earlier version.

---

You are continuing the reset-type test campaign. State of knowledge after
the first UDS-hardReset run (2026-08-07):

## Established — do not re-derive

- Safety-measure semantics (from firmware): on every reset the FW-check
  module classifies the reset class and runs a per-class expected-value
  table. Match -> clear SCU_RSTSTAT and SMU_AG0..AG11, allow Active.
  Deviation -> DO NOT clear, raise a Transition Prohibit Fault, pack
  cannot go Active. Clearing is CONDITIONAL - a previous version of this
  prompt wrongly assumed startup always clears; it does not.
- RSTCON = 0x00000100 statically (SW reset source = SYSTEM class),
  confirmed live on all three rigs. UDS ECUReset(hardReset) ->
  Mcu_PerformReset() -> SWRSTREQ runs entirely in CPU context - no
  protected debugger writes, no halt. Application class still has no
  qualified trigger (Path C escalation stands). The debugger-forced warm
  variant stays retired (probe-wedge history); the warm requirement
  already passed 2026-07-03.
- The rewritten test now carries a reboot witness (stored cause must
  CHANGE across the reset) - it worked: today's run failed honestly
  instead of false-passing.
- TODAY'S RESULT (dirty-baseline run, alarms latched pre-reset): the
  reset fired and hardware classified it correctly, but firmware never
  stored the new cause and never cleared RSTSTAT -> reboot witness
  failed. OPEN FORK, do not assume either side:
  (i) deviation branch working as designed - the dirty pre-reset state
      deviated from the SYSTEM expected-value table, so firmware
      correctly refused to clear and (expected) raised Transition
      Prohibit;
  (ii) defect in the UDS-reset -> FW-check path - store/clear silently
      skipped with no prohibit raised.
- HARD RULES unchanged: no debugger writes to ACCEN-protected space; do
  not halt CPU0 in normal operation (latches AG7[17]+AG10[17]); reads
  only over the debug port; never clear anything by hand.

## This slot — resolve the fork

1. POST-BOOT CAPTURE (reads only, non-intrusive attach): live
   SCU_RSTSTAT, SMU_AG0..AG11, the stored reset-cause copy, and the
   Transition-Prohibit / drive-block status. The prohibit status is the
   primary discriminator: PRESENT -> hypothesis (i); ABSENT while
   store/clear were skipped -> hypothesis (ii).
2. TABLE DIFF: identify which expected-value entries deviated - diff the
   captured registers against the per-class expected table for SYSTEM
   (firmware source or the self-check's own constants). Record the exact
   mismatching entries.
3. RECOVERY BY POWER CYCLE (the designed path back to clean): full power
   cycle of the DUT. Cold boot should match the cold table, letting
   firmware clear and reach a clean baseline. Do NOT fire repeated
   hardResets from the prohibited state - the deviation branch will keep
   refusing, by design.
4. CLEAN-BASELINE FORMAL RUN: verify alarm-clean + RSTSTAT-clean +
   prohibit absent, then fire UDS hardReset once. Expected: stored cause
   changes and decodes SYSTEM_RESET, RSTSTAT and AGs cleared by firmware,
   DUT reaches Active-capable state.
   - If it PASSES: formal <sysreq-system> specimen secured, AND today's
     dirty run is retroactively a live specimen of the deviation branch
     (do-not-clear + prohibit) - record it as such; that is bonus
     safety-measure evidence, not a failure.
   - If it FAILS from a clean baseline: genuine firmware defect in the
     UDS-reset -> FW-check path on this build - stop, write it up with
     both runs' evidence, escalate. Do not retry-loop.
5. Keep recording the <sysreq-application> escalation ask (test hook, or
   McuResetCfg=application config-variant build riding the proven UDS
   path) and the separately-filed vacuous ECUReset-behavior test finding.

## Safety rails

- Reads only over the debug port; no halting CPU0 in normal operation; no
  stubs, no Var.Call, no writes to ACCEN-protected space.
- Never clear an alarm or status register by hand - the conditional-clear
  behavior IS the mechanism under observation.
- One non-resetting attach per step; on failure run the probe-health
  check and STOP.
- Leave the DUT in Standby and the probe detached (NoDebug) at the end.
- Anything unexpected: stop, record, report - do not improvise.

## Deliverable

Short write-up: the post-boot capture with the prohibit-status verdict
(fork resolved to (i) or (ii)); the exact deviating table entries; the
power-cycle recovery record; the clean-baseline run evidence (or the
defect escalation with both runs attached); status of the application-
class ask. If any established fact above is contradicted live, say so
loudly and stop.
